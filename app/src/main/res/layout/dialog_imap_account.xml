<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView style="@style/SettingsLabel" android:text="Nome account (a piacere)" />
        <EditText android:id="@+id/edit_nickname" style="@style/SettingsInput" android:hint="es. Posta lavoro" />

        <TextView style="@style/SettingsLabel" android:text="Provider" />
        <Spinner android:id="@+id/spinner_provider" android:layout_width="match_parent" android:layout_height="wrap_content" />

        <TextView style="@style/SettingsLabel" android:text="Indirizzo email" />
        <EditText android:id="@+id/edit_email" style="@style/SettingsInput" android:inputType="textEmailAddress" />

        <TextView style="@style/SettingsLabel" android:text="Password (per Gmail: password per le app)" />
        <EditText android:id="@+id/edit_password" style="@style/SettingsInput" android:inputType="textPassword" />

        <TextView style="@style/SettingsLabel" android:text="Server IMAP" />
        <EditText android:id="@+id/edit_host" style="@style/SettingsInput" />

        <TextView style="@style/SettingsLabel" android:text="Porta" />
        <EditText android:id="@+id/edit_port" style="@style/SettingsInput" android:inputType="number" />

        <TextView style="@style/SettingsLabel" android:text="Sicurezza" />
        <Spinner android:id="@+id/spinner_security" android:layout_width="match_parent" android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/txt_test_result"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="@color/settings_dim"
            android:textSize="13sp" />

        <Button
            android:id="@+id/btn_test_connection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Verifica connessione" />

    </LinearLayout>
</ScrollView>
